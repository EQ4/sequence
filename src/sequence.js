(function(window) {
	"use strict";

	var assign = Object.assign;

	function Sequence(clock, data) {
		
	}

	assign(Sequence.prototype, Clock.prototype, {
		start: function(time) {
			
		},

		stop: function(time) {
			
		}
	});
})(window);